/**
 * @author      ATT 
 * @version     1.0
 * @since       2016-03-16
 */

//A single class containing an enum for every transaction - makes for better readability 
public enum TransactionName {LOGOUT, WITHDRAWAL, TRANSFER, PAYBILL, DEPOSIT, 
			     CREATE, DELETE, DISABLE, CHANGEPLAN, ENABLE, LOGIN}  
